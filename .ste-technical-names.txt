# Technical names permitted in this project (ASD-STE100 rule 1.4).
# One name for each line. Lines that start with # are comments.
#
# A technical name is permitted although it is not in the STE dictionary.
# Add a word only if it is a real technical name. Do not add a word only to
# make a checker result go away.
#
# Words that the checker flags but that this project keeps on purpose:
#   caution  - ASD-STE100 rule 6 makes this word necessary for a caution.
#   review   - part of "adversarial review", the procedure name of this project.
#   security - a technical term of software engineering.
#   speech   - part of "speech-to-text" and "text-to-speech".
#
# Known false results of the checker. The checker does not know the part of
# speech in the context. See references/parser-notes.md of the skill. These
# words are approved as nouns. The checker flags the verb entry.
#   file     - a data file (n). The dictionary refuses "to file" (v).
#   log      - a log (n). The dictionary refuses "to log" (v).
#   request  - part of the git name "pull request" (n).

# --- ASD-STE100 keywords and project procedure names ---
caution
warning
review
adversarial

# --- Known false results (noun sense is approved, verb sense is not) ---
file
log
request

# --- Languages, runtimes, and frameworks ---
dotnet
csharp
avalonia
xaml
python
javascript
jsx
react
vite
node
npm
nuget
xunit
mstest
bash

# --- Audio and typography technical names ---
# gain     - part of "software gain", the audio engineering term for a
#            multiplication of the signal level.
# variable - part of "variable font", the OpenType term for a font file with
#            axes. Avalonia cannot use one. See section 3.1.
gain
variable

# --- Machine learning and voice ---
gemma
litert
moonshine
kokoro
piper
llm
stt
tts
speech
inference
multilingual
transcription
translator
tokens
# prompt   - the LLM term for the instruction text sent to the model.
prompt

# --- Platform and hardware ---
# port     - a TCP port number, for example port 9379.
port
raspberry
pi
arm64
linux
debian
drm
kms
dsi
gpio
usb
alsa
pipewire
wpctl
pactl
amixer
lsof
systemd
kiosk
speakerphone
beamforming
mems
aec
agc
capacitive
powerbank
stl
firmware
ip64
dbspl

# --- Protocols and formats ---
http
https
json
wav
pcm
float32
base64
api
cors
url
uri
xml
# key - part of "API key", the name of the field in SettingsOverlay.jsx.
key

# --- Software engineering terms ---
software
security
architecture
readability
performance
interface
runtime
backend
frontend
subagent
tdd
syntax
event
mcp
wpf
xpf
mvvm
# injection - part of "dependency injection", the name of the pattern.
injection
# service - a part of the software that the container supplies. It is also the
#           systemd word for a unit. The dictionary approves "servicing" only,
#           which has a different meaning (maintenance work).
service

# --- Git and process terms ---
repository
fork
upstream
origin
branch
commit
merge
rebase
codeowners
cla

# --- Model names ---
opus
sonnet
fable

# --- CAD and OpenSCAD (section 12 of CLAUDE.md) ---
# manifold - the topology term for a surface with no hole and no self
#            intersection. It is also the name of the OpenSCAD engine.
# module   - the OpenSCAD keyword for a part of the source.
# iso      - the isometric view.
# insert - part of "heat-set insert", the name of the brass part that a hot
#          iron puts in a printed boss to give it a metal thread.
# screw  - a screw (n), the fastener. Also "screw bore", "screw boss", and
#          "screw terminals". The dictionary approves the verb "screw" only.
insert
screw
# well - the circular hole in the deck that holds the speakerphone. It is the
#        term of the source design and the name in the code (WELL_RI, well_2d).
#        The dictionary approves "well" as an adverb only.
# intake - the plate in the floor that draws cooling air into the enclosure.
#          It is the name in the code (INTAKE_POS, intake()). The dictionary
#          does not have this word.
well
intake
openscad
scad
bosl2
cad
cgal
manifold
mesh
facet
facets
geometry
slicer
boolean
module
png
3mf
iso
gui
powershell

# --- Domain terms of this project ---
case
lane
client
server
endpoint
proxy
offline
layout
portrait
landscape

# pogo - a spring-loaded contact pin. The X1201 supplies the Pi through four
#        of them. It is the vendor name for the part.
pogo

# --- names of the enclosure, added 2026-08-10 ---
#
# switch      - a switch (n) is a component that a person operates. The
#               dictionary refuses "to switch" (v) and gives SET (v). This is
#               the same class of false result as file, log, and request above.
#               The appliance has three: two push-to-talk and one for the
#               electrical supply.
# cover glass - the glass at the front of the display module. It is the name
#               that the Raspberry Pi document uses, and the perimeter of the
#               module is bare cover glass, which is what the rebate carries.
# rebate      - a step at the top of a wall that a part sits in. It is the
#               name of the feature in woodwork and in a machine shop.
# chamfer     - a face at 45 degrees at an edge. It is the name of the feature
#               in a machine shop.
switch
cover glass
rebate
chamfer

# --- names of the GPIO configuration ---
#
# momentary   - a momentary switch. It is the name of the type of the
#               component. The dictionary gives MOMENTARILY (adv), which is a
#               different part of speech.
# interrupt   - an interrupt (n) of the processor, which /proc/interrupts
#               counts. The dictionary refuses "to interrupt" (v). This is the
#               same class of false result as file, log, and request above.
# register    - a register (n) of a device on the I2C bus. The dictionary
#               refuses "to register" (v).
# state       - a state (n): the "state of charge" of a cell, and a state of
#               pinctrl. The dictionary refuses "to state" (v).
# fuel gauge  - the class of the device that measures the charge of a cell. It
#               is the name that Maxim and the Linux kernel both use.
# significant - part of "most significant byte", the name of the sequence of
#               the bytes of a value.
# repeated    - part of "repeated start", the name of the operation of I2C
#               that reads a register.
#
# swap        - swap (n), the memory facility of Linux. It is the name in
#               /proc/swaps, in swapon, and in the Mechanism value of
#               /etc/rpi/swap.conf. The dictionary refuses "to swap" (v). This
#               is the same class of false result as file, log, and request.
swap

# guard       - the low battery guard, which is the name of the component in
#               deploy/gemma-battery-guard.sh, in the name of its unit, and in
#               its Description. The dictionary refuses "to guard" (v). This is
#               the same class of false result as file, log, and request above.
guard

# --- the types of Conventional Commits ---
#
# fix         - the type of a commit that corrects a defect, in the first line
#               of a commit message. It is a token of Conventional Commits and
#               not the verb. The other types that this repository uses (feat,
#               docs, refactor, chore) are not in the dictionary, thus the
#               checker does not examine them and they need no entry here.
fix
momentary
interrupt
register
state
fuel gauge
significant
repeated

# --- names of the user interface ---
#
# design       - the design (n), which Claude Design made. It is the artefact
#                that specifies the user interface, and the name of the tool
#                that made it. The dictionary approves the verb only.
# medium       - Medium, the name of the weight 500 of a font. It is a value
#                of Avalonia FontWeight and part of a file name.
# idle         - Idle, the name of the state where the appliance waits for a
#                button. It is the term of the brief and a value of the state.
# screensaver  - the screen that comes after a quiet interval. It is the term
#                of the brief.
# drum         - the language selector. The list turns behind a small window,
#                like the drum of a revolver. It is the term of the brief and
#                of the upstream component LanguageLane.jsx.
# visualizer   - the bars that show the level of the audio. It is the name of
#                the upstream component Visualizer.jsx.
# accent       - the accent colour, which a person selects. It is the name of
#                the token in the design and of the row in the settings.
# theme        - the theme (n) of the display: the light variant and the dark
#                variant. It is the name of the Avalonia facility
#                (ThemeVariant, ThemeDictionaries).
# overlay      - a layer above the surface. The DRM backend makes no popup,
#                thus each dialog is an overlay. It is also the name of the
#                upstream component SettingsOverlay.jsx and of the device tree
#                overlay of the GPIO configuration.
# glyph        - the shape of one character in a font. It is the name in
#                Avalonia GlyphTypeface and in OpenType.
# bitmap       - an image of pixels. It is the name of the Avalonia type.
# headless     - the Avalonia platform that makes no window. It is the name of
#                the package Avalonia.Headless.
# screenshot   - an image of the display, which the harness writes to a file.
# harness      - the software that starts the user interface and writes a
#                screenshot. It is not a test project. See section 5.2.
# battery      - the two cells of the appliance, and the name that the display
#                shows. The Linux power_supply class uses this word.
# splash       - the image that the system shows before the software starts.
design
medium
idle
screensaver
drum
visualizer
accent
theme
overlay
glyph
bitmap
headless
screenshot
harness
battery
splash

# --- names of the controls that we write ---
#
# brush - a brush (n), the Avalonia type that fills a shape (IBrush,
#         SolidColorBrush). The dictionary refuses "to brush" (v). This is the
#         same class of false result as file, log, and request above.
# bolt  - the shape of a flash of lightning, which the glyph of the battery
#         shows while the cells take charge. It is the name of the shape in the
#         design and the name of the identifier in the code.
brush
bolt

# --- names of the user interface work, second group ---
#
# branding  - part of the file name Assets/Branding.axaml. The checker sees an
#             "-ing" form. Section 8 exempts a file name from STE.
# binding   - a binding (n) of Avalonia, which connects a property of a view to
#             a property of a view model. The checker sees an "-ing" form. It is
#             the name of the facility (Binding, CompiledBinding).
# padding   - Padding, the property of a control of Avalonia that gives the
#             space inside its border. The checker sees an "-ing" form.
# store     - the store (n) of the settings, which is IUserSettingsStore. The
#             dictionary approves the verb only.
# thread    - a thread (n) of the process. "The thread of the user interface" is
#             the name of the one thread that Avalonia permits to touch a
#             control. The dictionary approves the verb only.
# recording - a Recording (n), the type that holds the samples of one push of a
#             button. The checker sees an "-ing" form.
# danger    - the condition of the cells at 10 % and less. It is the name in the
#             code (IsDanger, BatteryDangerBrush) and in the design.
# list      - a list (n): a fallback list of fonts, a list of the languages. The
#             dictionary approves the verb only. This is the same class of false
#             result as file, log, and request above.
# process   - a process (n) of the operating system. litert-lm operates in a
#             process of its own, which systemd starts. The dictionary approves
#             PROCEDURE (n) and DO (v), which have a different meaning.
branding
binding
padding
store
thread
recording
danger
list
process

# entry point - the method that the runtime calls first. It is the term of the
#               C# specification and of the .NET documents.
entry

# --- names of USB HID telephony call control ---
#
# hook - part of "off hook", "on hook", and "Hook Switch". It is the term of
#        the telephony domain and the name of usage 0x20 of the Telephony
#        page of the HID Usage Tables. The dictionary approves PUT (v), which
#        has a different meaning.
# call - a call (n), the state that the host writes to the device with the
#        Off-Hook LED. "Call control" is the name of the standard contract in
#        the HID Usage Tables. The dictionary approves the verb only, which is
#        the same class of false result as file, log, and request above.
hook
call

# --- names of the text-to-speech work ---
#
# working      - AppState.Working, the state while the model and the speech
#                server do their work. It is the same class as idle and
#                screensaver above. The checker sees an "-ing" form.
working

# --- names of the thread of the conversation ---
#
# exchange - an Exchange (n): one turn of the conversation, which is the words
#            of a person and the translation of them. It is the name of the
#            type, and the name in the log message "The exchange is complete".
#            The dictionary approves INTERCHANGE (v) and REPLACE (v), which
#            have a different meaning.
# extent   - ScrollViewer.Extent, the Avalonia property that gives the size of
#            the content that a person can scroll. The pin of the thread reads
#            this property. The dictionary approves DEPTH, LENGTH, and WIDTH,
#            which are the dimensions of a part and not this property.
exchange
extent

# --- names of the settings ---
#
# setting  - the name of this concept in the software: UserSettings,
# settings   IUserSettingsStore, appsettings.json, SettingsViewModel, and the
#            settings screen that a person opens with the gear. The dictionary
#            approves ADJUSTMENT (n) and POSITION (n), and neither one is the
#            name of the type. Section 3.2 of CLAUDE.md uses the word for the
#            same concept.
# true     - the two literals of a bool in C#. A commit message that gives the
# false      value of a bool must give the literal that the code holds, and
#            CORRECT and INCORRECT are a different statement.
setting
settings
true
false

# user - the person who operates the software. It is the word of this domain:
#        software has users, and "user interface" is the name of the part that
#        this fork replaces. ASD-STE100 does not hold the word, thus it is a
#        technical name of rule 1.4.
#
#        Use "user" for the human at the appliance. Keep "person" for a human
#        who is not a user, for example a bystander who must see that the
#        microphone is open.
user
